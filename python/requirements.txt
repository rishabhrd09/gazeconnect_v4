# GazeConnect Pro - Python Backend Dependencies
# Install with: pip install -r requirements.txt

websockets>=12.0
pyttsx3>=2.90
pyautogui>=0.9.54
comtypes>=1.2.0

# Floor plan generator + API dependencies
flask>=3.0.0
flask-cors>=4.0.0
pycairo>=1.25.0
ezdxf>=1.3.0
svgwrite>=1.4.0
Pillow>=10.0.0
numpy>=1.24.0

# Web Hub dependencies
aiohttp>=3.9.0

# GazeConnect Neural Language Model (CIFG-LSTM)
# Training: pip install torch (only needed for training, not inference)
# Inference: onnxruntime is lightweight (~15MB) and runs on CPU
onnxruntime>=1.16.0

# Optional (v5 AI layout engine)
shapely>=2.0.0
networkx>=3.0
squarify>=0.4.4
ortools>=9.10.0
